# Render Credentials
# Generate a new Render API key in your Account Settings -> API Keys
RENDER_API_KEY=your_new_render_api_key_here

# LLM API Credentials
# Replace these with your active keys (since hardcoded keys were committed, they may have been revoked by Git's secret scanner)
OPENROUTER_API_KEY=your_openrouter_api_key_here
GEMINI_API_KEY=your_gemini_api_key_here
